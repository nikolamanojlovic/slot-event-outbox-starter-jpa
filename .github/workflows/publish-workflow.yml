name: Build and Publish Slot Event Outbox Starter JPA

on:
  push:
    branches:
      - main

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: 21
          distribution: 'temurin'
          server-id: github
          server-username: nikolamanojlovic
          server-password: ${{ secrets.SLOT_TOKEN }}

      - name: Build JAR
        run: ./mvnw clean package

      - name: Publish JAR to GitHub Packages
        run: ./mvnw deploy -DskipTests
        env:
          GITHUB_TOKEN: ${{ secrets.SLOT_TOKEN }}
